import Head from 'next/head';
import MdxLayout from '../../components/layouts/mdxLayout';
import SourceCondition from '../../components/docs/sourceCondition.mdx';
import Destination from '../../components/docs/destination.mdx';
import TextBG from '../../components/docs/textBG.mdx';

<Head>
  <title>Inject File</title>
  <meta name="description" content="Inject HTML CSS or JAVASCRTIPT file any webpage before content load or after load" />
</Head>

<h1 className="text-4xl border-b border-neutral-200/20 pb-2 text-neutral-300">Inject File</h1>

<br/>

The Inject File feature enables the injection of three types of files into webpages dynamically, whether they are remote or local files.

<br/>

<img className="w-full rounded" alt="inject file" src="/assets/images/rules-image/inject-file.png" />

<br/>

<SourceCondition />

<br/>

<span className="text-neutral-200">**File Type:**</span>
Here need to select File Type like <TextBG text="HTML" />, <TextBG text="CSS" /> or <TextBG text="JAVASCRIPT" />.

<br />

<span className="text-neutral-200">**Source Type:**</span>
Here need to select Source Type <TextBG text="CODE" /> or <TextBG text="URL" />.\
If select CODE, need to utilize the editor to inject the written code as a file.\
If select URL, need to provide a URL to inject the returned data.


<br/>

<div className="flex justify-between mt-20 mb-10">
  <a
    className="flex items-center w-fit h-10 p-px px-3 py-2 space-x-2 text-sm no-underline transition duration-200 cursor-pointer text-slate-100 bg-sky-600 hover:bg-sky-400 font-semiboldtext-center ustify-center group hover:shadow-2xl shadow-zinc-900 rounded-2xl"
    href="/docs/modify-response"
  >
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" className="w-6 h-6">
      <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5L3 12m0 0l7.5-7.5M3 12h18" />
    </svg>
    Modify Response
  </a>
  <a
    className="flex items-center w-fit h-10 p-px px-3 py-2 space-x-2 text-sm no-underline transition duration-200 cursor-pointer text-slate-100 bg-sky-600 hover:bg-sky-400 font-semiboldtext-center ustify-center group hover:shadow-2xl shadow-zinc-900 rounded-2xl"
    href="/docs/modify-request-body"
  >
    Modify Request Body
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" className="w-6 h-6">
      <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
    </svg>
  </a>
</div>

export default function MDXPage({ children }) {
  return <MdxLayout>{children}</MdxLayout>;
}
